# Copy this file to .env and fill in the values for your local CUA deployment

# REQUIRED: Container image for the CUA computer/server
# Get the official image reference from the CUA documentation or your registry
CUA_IMAGE=REPLACE_WITH_CUA_COMPUTER_IMAGE

# REQUIRED: API key for your local CUA server (if the image requires one)
CUA_API_KEY=REPLACE_WITH_CUA_API_KEY

# OPTIONAL: Workspace ID (if applicable)
CUA_WORKSPACE_ID=

# Host port to expose the CUA HTTP API on your machine
CUA_PORT=8000

# Container internal port where the CUA HTTP API listens
# Set this to the port exposed by the image (commonly 8000, but check docs)
CUA_INTERNAL_PORT=8000
